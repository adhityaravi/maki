[project]
name = "maki"
version = "0.0.1"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["pkgs/*"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["pkgs"]
